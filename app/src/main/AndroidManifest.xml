<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mx.uach.hcilab.kinectlogger">

    <!-- FOR Storage -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!-- FOR Camera -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.FLASHLIGHT" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TherapistActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".PatientManagerActivity"
            android:screenOrientation="landscape" />
        <activity
            android:name=".TherapistManagerActivity"
            android:screenOrientation="landscape" />
        <activity
            android:name=".ReflexRidgeActivity"
            android:screenOrientation="landscape">
        </activity>
        <activity android:name=".LeaksActivity">
        </activity>
        <activity android:name=".RallyBall" />
        <activity android:name=".RiverRushActivity" />
        <activity android:name=".GameSelectorActivity"></activity>
    </application>

</manifest>